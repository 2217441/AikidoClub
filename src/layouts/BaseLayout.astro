---
import '../styles/global.css';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import ScrollToTop from '../components/ScrollToTop.astro';

const base = import.meta.env.BASE_URL;

interface Props {
  title: string;
  description?: string;
  currentPath?: string;
}

const { 
  title, 
  description = 'Aikido Club - Martial arts training and community',
  currentPath = Astro.url.pathname 
} = Astro.props;

const site = Astro.site;
const pageUrl = new URL(currentPath, site).href;
const ogImage = new URL(`${base}img/og-cover.jpg`, site).href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />

    <!-- Social share previews (WhatsApp, Facebook, Telegram, Slack...) -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="IIUM Aikido Club" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={pageUrl} />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    
    <!-- Favicons -->
    <link rel="icon" type="image/png" sizes="32x32" href={`${base}favicons/favicon-32x32.png`} />
    <link rel="icon" type="image/png" sizes="16x16" href={`${base}favicons/favicon-16x16.png`} />
    <link rel="apple-touch-icon" sizes="180x180" href={`${base}favicons/apple-touch-icon.png`} />
    <link rel="manifest" href={`${base}favicons/site.webmanifest`} />
    
    <!-- Fonts are self-hosted: see src/styles/fonts.css. No third-party
         requests, no render-blocking stylesheet. Preloaded: body (400)
         and heading (700) latin faces. -->
    <link rel="preload" href={`${base}fonts/S6uyw4BMUTPHjx4wXg.woff2`} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={`${base}fonts/S6u9w4BMUTPHh6UVSwiPGQ.woff2`} as="font" type="font/woff2" crossorigin />
    
    <title>{title}</title>
  </head>
  <body class="bg-bg-dark text-white font-['Lato',Arial,sans-serif]">
    <div class="page-wrapper w-full min-h-screen flex flex-col">
      <Nav currentPath={currentPath} />
      
      <main id="main-content" class="flex-1">
        <slot />
      </main>
      
      <Footer />
      <ScrollToTop />
    </div>
  </body>
</html>
